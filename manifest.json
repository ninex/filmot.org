{
  "manifest_version": 2,

  "name": "filmot.org",
  "description": "Change imgur.com to filmot.org. Supports RES view images and never ending reddit. Changes albums to gallery view so they work",
  "version": "1.3",
  "content_scripts": [{
    "matches": ["<all_urls>"],
	"js": ["filmot.js"]
  }]
}